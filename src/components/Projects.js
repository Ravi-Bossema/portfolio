import React from 'react';
import { useState } from 'react';
import { Box } from '@mui/material';

export default function Projects() {
	return (
		<Box sx={{ minHeight: window.innerHeight, backgroundColor: 'red' }} />
	);
}
